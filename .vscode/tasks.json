// Sass configuration
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "Sass Compile",
            "type": "shell",
            "command": "node-sass",
            "args": ["./src/styles.scss", "./src/styles.css", "--output-style compressed"],
            "group": "build"
        }
    ]
}
